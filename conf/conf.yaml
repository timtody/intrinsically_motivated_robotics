main:
  env_name: reach_target-state-v0
  max_timesteps: 50000
  train_each: 100
  video_length: 1000
  video_folder: vid
  video_name: ${main.video_folder}/recording_ 
  video_format: .mp4
  pc_each: ${main.video_length}
  norm_scale: 0.25

wandb:
  use: False
  project: collision
  name: test

ppo:
  #n_discrete: 7
  n_latent_var: 256
  lr: 0.002
  betas: [0.9, 0.999]
  K_epochs: 4
  eps_clip: 0.2
  gamma: 0.99
  max_action: 1

env:
  scene_path: base.ttt
  headless: True
  state: low